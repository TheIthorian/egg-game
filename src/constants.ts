/** Number of eggs available when a new session starts. */
export const STARTING_EGG_COUNT = 40;
/** Number of eggs offered by the purchase popup when the nest is empty. */
export const STORE_EGG_COUNT = 80;

/** Base number of eggs consumed per spawn before any multiplier is applied. */
export const BASE_EGG_DEDUCTION_AMOUNT = 1;
/** Default multiplier applied to egg consumption before any purchase changes it. */
export const DEFAULT_EGG_DEDUCTION_FACTOR = 1;

/** Multiplier applied to egg consumption after the player buys more eggs. */
export const PURCHASED_EGG_DEDUCTION_FACTOR = 40;

/** Minimum score increase required before the score popup can trigger again. */
export const MIN_SCORE_POPUP_DELTA = 60;
/** Maximum score increase required before the score popup can trigger again. */
export const MAX_SCORE_POPUP_DELTA = 240;
